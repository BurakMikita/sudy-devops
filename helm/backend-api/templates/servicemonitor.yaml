apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-api-monitor
  labels:
    release: {{ .Values.monitoring.prometheusRelease }}   # ← должен совпадать с именем release Helm
spec:
  selector:
    matchLabels:
      app: backend-api
  endpoints:
    - port: http      # имя порта из Service
      path: /metrics
      interval: 15s
